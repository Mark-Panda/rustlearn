syntax = "proto3";
package grpc.service;

// 请求消息
message UserRequest {
    string request_msg = 1;
}

// 响应消息
message UserResponse {
    string message = 1;
}

// 定义服务
service UserGrpcService {
    // 一个简单的 RPC 方法
    rpc SayUserHello(UserRequest) returns (UserResponse);
}